// Content in :
// Main Script
// Setup
// Loop

void Wirelss_Button(void) {

/*

  int Tick_Counter;

  while (Wireless_Button_A == HIGH) {
    Tick_Counter ++;
    delay(250);
    if (Tick_Counter > 3) {
      Serial_CLI_Relay(3, Serial_CLI_Relay_Begin_Pin, Serial_CLI_Relay_End_Pin);
      return;
    }
  }

  while (Wireless_Button_B == HIGH) {
    Tick_Counter ++;
    delay(250);
    if (Tick_Counter > 3) {
      Serial_CLI_Relay(2, Serial_CLI_Relay_Begin_Pin, Serial_CLI_Relay_End_Pin);
      return;
    }
  }
    
  while (Wireless_Button_C == HIGH) {
    Tick_Counter ++;
    delay(250);
    if (Tick_Counter > 3) {
      Serial_CLI_Relay(6, Serial_CLI_Relay_Begin_Pin, Serial_CLI_Relay_End_Pin);
      return;
    }
  }

  while (Wireless_Button_D == HIGH) {
    Tick_Counter ++;
    delay(250);
    if (Tick_Counter > 3) {
      Serial_CLI_Relay(8, Serial_CLI_Relay_Begin_Pin, Serial_CLI_Relay_End_Pin);
      return;
    }
  }

*/

} // End of Wirelss_Button

